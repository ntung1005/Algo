#include<bits/stdc++.h>
using namespace std;

struct node{
	int val,level;
};

int main(){
	int t;
	cin>>t;
	while(t--){
		int s,f;
		set<int> set;
		
		cin>>s>>f;
		
		queue<node> q;
		
		node n = {n,0};
		q.push(n);
		while(!q.empty()){
			node tmp = q.front();
			q.pop();
			
			if(t.val == f){
				
			}
		}
		
	}
}
